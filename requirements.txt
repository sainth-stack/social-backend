fastapi>=0.110.0
uvicorn[standard]>=0.24.0

# Database — PostgreSQL
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

pydantic-settings>=2.0.0
email-validator>=2.0.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[pbkdf2-sha256]>=1.7.4
python-multipart>=0.0.9
cryptography>=42.0.0

httpx[http2]>=0.25.0

# Async workers — Redis as Celery broker
celery[redis]>=5.3.0
redis>=5.0.0

# LLM + image/video generation — Azure OpenAI (gpt-4o-mini / gpt-image-2 / Sora 2)
openai>=1.0.0

# Object storage — Azure Blob Storage
azure-storage-blob>=12.19.0
azure-core>=1.30.0

# Image processing — reference images for Sora 2 (must match video resolution)
Pillow>=10.0.0

# Tests
pytest>=8.0.0
